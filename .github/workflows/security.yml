name: Security Audit (Manual)

on:
  workflow_dispatch: # Eliminamos push/pull_request para quitar la X roja y el spam de fallos

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Report
        run: echo "Audit pipeline ready for manual execution."
